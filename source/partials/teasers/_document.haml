.views-row
  %article.node-teaser-document.node-teaser
    .field-type-image
    .field-type-wrapper
      %h2.node-title
        %a{:href => "#"}
          = truncate(lorem.sentence, :length => 75)
      .submitted
        %a.type.document{:href => "#"} Document
        24 Sep 2013, by
        - if current_page.data.user_role != "an"
          %a{:href => "#"}
            = lorem.name
        - else
          = lorem.name.downcase.tr(' ', '_')
      .field-name-body
        =lorem.paragraph
