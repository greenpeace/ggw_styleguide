- data.photos.album_one.each do |item|
  .views-row
    %article.node-teaser-photo.node-teaser
      .field-type-image
        %a{:href => "./photo-album.html"}
          = partial :"teasers/teaser-image", :locals => {:image => "#{item.image}"}
      .field-type-wrapper
        %h2.node-title
          %a{:href => "./photo-album.html"}
            = item.title
        .submitted
          %a.type.document{:href => "./photos.html"} Photos
          = succeed ", by" do
            = item.date
          - if current_page.data.user_role != "an"
            %a{:href => "./profile.html"}
              = item.author
          - else
            = item.author.downcase.tr(' ', '_')
          - if current_page.data.user_role != "an"
            - if item.comment_amount
              %a.teaser-comments{:href => "./photo-album.html#block-comments"}
                = item.comment_amount
        .field-name-body
          = item.introtext
