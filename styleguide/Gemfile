source 'https://rubygems.org'

group :development do

  # Sass, Compass and extensions.
  gem 'sass-globbing', ">= 1.1.0"   # Import Sass files based on globbing pattern.
  gem 'compass', '~> 1.0.0.alpha.19'# Framework built on Sass, imports Sass.
  gem 'compass-normalize'           # The attribute normalizer
  gem 'singularitygs', '~> 1.2.1'   # Advanced responsive grid system for Sass and Compass
  gem 'toolkit', '~> 2.3.0'         # Toolkit full of bits and bobs of awesome aimed at assisting in advanced styling with Sass
  gem 'breakpoint', '~> 2.4.2'      # Really simple media queries in Sass
  gem 'oily_png', '~> 1.1.1'        # Faster Compass sprite generation.
  gem 'css_parser', '~> 1.3.5'      # A set of classes for parsing CSS in Ruby.
  gem 'yajl-ruby'               # Faster JSON with LiveReload in the browser.

  # Dependency to prevent polling. Setup for multiple OS environments.
  # Optionally remove the lines not specific to your OS.
  # https://github.com/guard/guard#efficient-filesystem-handling
  gem 'rb-inotify', '~> 0.9', :require => false      # Linux
  gem 'rb-fsevent', :require => false                # Mac OSX
  gem 'rb-fchange', :require => false                # Windows

end
